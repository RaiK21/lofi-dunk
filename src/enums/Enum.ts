export enum Dirs {
    LEFT = "LEFT",
    RIGHT = "RIGHT",
    UP = "UP",
    DOWN = "DOWN",
}

export enum GlobalEvent {
    SCORE = "score",
}

export enum GameState {
    READY = "ready",
    SCORING = "scoring",
    OVER = 'over'
}