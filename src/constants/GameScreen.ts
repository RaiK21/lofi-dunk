export default class GameScreen {
    static WIDTH = 720;
    static HEIGHT = 1280;
    static CENTER_X = this.WIDTH / 2;
    static QUARTER_X = this.WIDTH / 4;
    static CENTER_Y = this.HEIGHT / 2;
    static QUARTER_Y= this.HEIGHT / 4;
    static LEFT = 0;
    static RIGHT = this.WIDTH;
    static TOP = 0;
    static BOTTOM = this.HEIGHT;
  }